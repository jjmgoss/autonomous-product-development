# Shared Artifacts

Use this directory for reusable outputs that are not specific to one run.

Most generated reports and evaluation outputs should live under `artifacts/runs/<run-id>/`.

Reusable prototype scaffolds, shared demo fixtures, and other build-forward assets that are meant to be copied into generated projects belong here.