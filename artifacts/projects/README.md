# Generated Projects

This directory is reserved for generated software projects that were explicitly approved after discovery and validation.

Do not place discovery-run artifacts here.

When a project starts from scratch, prefer the shared scaffold under `artifacts/shared/prototype-scaffold/` or `python scripts/init_prototype_scaffold.py` instead of reinventing the local project structure each time.