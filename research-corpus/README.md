# Research Corpus

This directory stores saved external evidence used during discovery and validation.

## Layout

- `runs/` stores run-scoped evidence bundles.
- `shared/` stores reusable source material that legitimately applies across runs.

Each run should use the structure described in `agent/research-corpus-conventions.md`.

The important rule is simple: strong claims in discovery artifacts should be traceable back to saved evidence in this directory.